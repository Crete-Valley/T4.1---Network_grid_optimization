apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svc
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: dpsv  # Matches the label on your pod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP